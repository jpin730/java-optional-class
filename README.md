# Java Optional Class

This repository contains examples and explanations of the Java Optional class, which is a container object that may or
may not contain a non-null value. It is used to represent optional values and to avoid null pointer exceptions in Java.